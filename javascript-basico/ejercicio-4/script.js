// Escribir una función que tome una cadena (a) y un número (n) como argumento
// Devuelve el carácter enésimo de 'a'

function myFunction(a, n) {
  return a[n - 1];
}
console.log(myFunction("holamaria", 5));
